import Vue from 'vue'
import axios from 'axios'

// initial state
const state = {
}

// getters
const getters = {
}

// actions
const actions = {
	
}

// mutations
const mutations = {
	setdata(state,data) {
		state = Object.assign(state,data)
	}
}

export default {
  state,
  getters,
  actions,
  mutations
}